__pycache__/
*.py[cod]

# API keys for the VLM propose step — never committed
key.env

# Local environment
.venv/
.env

# generated by `extract.py emit-sampler`, and stale the moment the KB path changes
_generated_sampler.cs

# eval traces are run artifacts, not source
*_trace.json
_traces/
