# Claude-authored backup (do not use / do not delete)

Snapshot of the 8 accepted MotionKB actions as AUTHORED by claude-opus-4-8 (ADR 0008), taken before
re-authoring the same 8 with gpt-5.5-2026-04-23. The pipeline does NOT read these; they are kept only
as a reference backup of the prior authoring. MEASURED was identical either way (program-produced).
